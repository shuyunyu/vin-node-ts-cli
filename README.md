# A cli for create ***`node`+`ts`*** project.

### Install
```sh
npm install vin-node-ts-cli -g
```

### Create Project
```sh
mkdir test-project-path
cd test-project-path

# create node+ts project
vinnt create test-node-tes-project

cd test-node-tes-project
git init
```
